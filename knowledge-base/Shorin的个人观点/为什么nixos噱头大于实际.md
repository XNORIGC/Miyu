NixOS的声明式、可复现的特点很棒。但是声明式强绑定了nix语言，而nix语言是一门用户友好的语言，学习成本很高。可复现这一点对于服务器批量部署有用，但是对于桌面端用户意义不大。我如果想复现，写个bash脚本，再花个20分钟一样能复现，我可能一年都不会重装一次，我的电脑也不超过三台，没必要功夫为了所谓的可复现去学习新语言。再说了，如果我想要在不同的设备上装不同的配置，你nixos的可复现不就成笑话了？
